const fs = require('fs');
const SparkMD5 = require('spark-md5');

const xml = fs.readFileSync('src/utils/__fixtures__/3633084_SADTxml.xml', 'utf8'); // Wait, use the same stringValores
// Or actually I don't have the same string.
